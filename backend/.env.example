DATABASE_URL=postgresql://wkpoule:wkpoule@localhost:5432/wkpoule
JWT_SECRET_KEY=change-me-in-production-use-openssl-rand-hex-32
# Production (OpenShift): https://your-route-host (matches browser URL; used in emails + CORS)
PUBLIC_APP_URL=http://localhost:5173
# Optional comma-separated extra CORS origins (leave empty to use localhost + PUBLIC_APP_URL)
CORS_ORIGINS=
WEATHER_API_KEY=
FOOTBALL_DATA_API_KEY=
